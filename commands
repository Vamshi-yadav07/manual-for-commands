git init
git remote add origin url
git add -A
  git commit -m 'message'
  git branch -M main
git push -u origin main

// if not runs on vs code
"scripts": {
 "start": "react-scripts --openssl-legacy-provider start",
 ...
}
